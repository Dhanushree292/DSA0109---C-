#include <iostream>
#include <string>
using namespace std;

int main() {
    string str = "Hello, SIMATS!";
    string *ptr = &str;

    cout << "Value of string using pointer: " << *ptr << endl;

    return 0;
}
